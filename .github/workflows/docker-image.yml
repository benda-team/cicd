name: CICD To Server

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Install SSH key
      uses: webfactory/ssh-agent@v0.8.0
      with:
        ssh-private-key: ${{ secrets.MY_LAPTOP_SSH_PRIVATE_KEY }}

    - name: Deploy to CentOS server
      run: |
        ssh root@1.53.82.154 "mkdir demofromgitaction"
